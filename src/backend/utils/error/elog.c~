void
elog(int lev, char *fmt, ...)
{
  va_list  ap;
  char     buf[ELOG_MAXLEN], line[ELOG_MAXLEN];
  register char *bp, *cp;
  extern   int errno, sys_nerr;
  time_t   tim, time();
  int      len;
  int      i = 0;

















}
